# Mirror status

- Upstream: https://github.com/1234567Yang/cf-proxy-ex
- Native GitHub fork API: 403 (Resource not accessible by integration)
- This repo: created via create_repository as a stand-in mirror
- Cloudflare Worker `proxy` deployed from upstream `_worker.js` via Workers Scripts API
- Production password set to empty string to preserve prior no-password behavior; upstream default remains `123`
